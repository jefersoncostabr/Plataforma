-Pegar ultimo commit
git reset --hard HEAD

-Pegar commit específico
git reset --hard 8c4e92

-Log dos commits
git log --oneline

-Log da branch atual
git log --oneline --decorate --no-merges


*Pegar para a main(merge)
git checkout main
git pull origin main
git merge <branch>
git add .
git commit
git push origin main

*codigo no console do navegador
window.toggleDebugVelocidadePlayer()
window.apagarBasePersistidaDev()
* testar gamepad
gp.mostrarEstadoGamepad()      // Live gamepad state + buttons
gp.mostrarMapeamentos()        // Action→button mappings
gp.simularAcao('pulo')         // Test action without hardware
gp.mostrarAcoesAtivas()        // Currently active actions
gp.testarConectividade()   

*Código de terminal para pegar dados do git
git config --get user.email; git config --get user.name

https://gamepad-tester.com/

node ".\tools\editor\editor-save-server.js"